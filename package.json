{
  "scripts": {
    "start": "rimraf public && parcel src/index.html",
    "build": "rimraf dist && parcel build ./src/app.js --no-source-maps"
  },
  "devDependencies": {
    "parcel": "^2.0.0-beta.2",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "jquery": "^3.6.0",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
